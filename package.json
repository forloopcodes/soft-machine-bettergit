{
  "name": "soft-machine-bettergit",
  "private": true,
  "version": "3.0.0",
  "description": "GitHub PRs & Issues panels for Soft-Machine, backed by the workspace's GitHub connection",
  "scripts": {
    "test": "vitest run",
    "smoke": "node scripts/smoke-bridge.mjs",
    "sync-manifest": "node scripts/sync-manifest.mjs",
    "check-manifest": "node scripts/sync-manifest.mjs --check"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "prismjs": "^1.30.0"
  }
}
